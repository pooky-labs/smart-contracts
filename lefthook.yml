colors: false
no_tty: true

pre-commit:
  parallel: true
  commands:
    artifacts:
      run: |
        NO_COLOR=1 forge compile
        pnpm artifacts
    test:
      run: NO_COLOR=1 forge test
    lint:
      glob: '*.sol'
      run: ./node_modules/.bin/solhint '{src,test}/**/*.sol'
